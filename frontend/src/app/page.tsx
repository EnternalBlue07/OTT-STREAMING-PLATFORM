import { HomeExperience } from "@/features/home/HomeExperience";

/**
 * Homepage (server component shell). Renders the Phase 1 cinematic discovery
 * experience: parallax hero, mood discovery, momentum rails, and Taste DNA —
 * all driven by the FastAPI content API.
 */
export default function HomePage() {
  return <HomeExperience />;
}
